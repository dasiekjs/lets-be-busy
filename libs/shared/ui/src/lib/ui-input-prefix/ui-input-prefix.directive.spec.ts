import { UiInputPrefixDirective } from './ui-input-prefix.directive';

describe('UiInputPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new UiInputPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
