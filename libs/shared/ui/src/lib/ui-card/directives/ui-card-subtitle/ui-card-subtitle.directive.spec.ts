import { UiCardSubtitleDirective } from './ui-card-subtitle.directive';

describe('UiCardSubtitleDirective', () => {
  it('should create an instance', () => {
    const directive = new UiCardSubtitleDirective();
    expect(directive).toBeTruthy();
  });
});
