import { UiCardActionsDirective } from './ui-card-actions.directive';

describe('UiCardActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new UiCardActionsDirective();
    expect(directive).toBeTruthy();
  });
});
