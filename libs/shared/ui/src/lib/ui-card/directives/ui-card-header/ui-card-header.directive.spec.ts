import { UiCardHeaderDirective } from './ui-card-header.directive';

describe('UiCardHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new UiCardHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
