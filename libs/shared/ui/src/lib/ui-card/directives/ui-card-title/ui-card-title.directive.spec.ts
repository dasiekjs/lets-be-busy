import { UiCardTitleDirective } from './ui-card-title.directive';

describe('CardTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new UiCardTitleDirective();
    expect(directive).toBeTruthy();
  });
});
