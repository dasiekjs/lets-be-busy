import { UiInputContainerDirective } from './ui-input-container.directive';

describe('UiInputContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new UiInputContainerDirective();
    expect(directive).toBeTruthy();
  });
});
