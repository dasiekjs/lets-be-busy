import { UiInputLabelDirective } from './ui-input-label.directive';

describe('UiInputLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new UiInputLabelDirective();
    expect(directive).toBeTruthy();
  });
});
