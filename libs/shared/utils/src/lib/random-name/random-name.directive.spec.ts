import { RandomNameDirective } from './random-name.directive';

describe('RandomNameDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomNameDirective();
    expect(directive).toBeTruthy();
  });
});
